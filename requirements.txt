# Tools that run directly inside the Codespace (not inside the Airflow
# Docker containers). Airflow's own dependencies live in the docker-compose
# stack, not here -- see devcontainer.json for the rationale.
apache-airflow-providers-snowflake
requests
pendulum